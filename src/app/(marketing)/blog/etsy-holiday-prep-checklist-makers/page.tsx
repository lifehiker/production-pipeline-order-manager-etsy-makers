export const dynamic = 'force-static';
export const revalidate = false;

export default function BlogPostTwo() {
  return <main className="mx-auto max-w-3xl px-6 py-14"><h1 className="text-5xl font-semibold tracking-tight">Etsy holiday prep checklist for makers</h1><p className="mt-6 text-lg leading-8 text-[var(--muted-ink)]">Audit production capacity, confirm shipping cutoffs, create intake forms for custom work, and set batch targets before August turns into a backlog.</p></main>;
}
